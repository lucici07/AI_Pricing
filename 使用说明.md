# GA Repricing Platform 原型 · 使用说明

> 演示地址:<https://lucici07.github.io/AI_Pricing/>
> 纯前端单文件(`index.html`),不用安装、不用登录、不连任何后端。所有账户名、PN、数字都是**虚构演示数据**。

---

## 1. 这是什么

一个可点击的流程原型,演示《GA Repricing Process Overview》里"Solution"部分的落地形态。它把整个流程拆成三个平台:

| Tab | 对应真实平台 | 干什么 |
|---|---|---|
| **LBP · RPT** | Lenovo Bid Platform + 嵌在里面的 Repricing Tool | Sale 提单;Pricer / PM / Director 领单、算价、审批 |
| **Tracker** | PowerApps(独立 app,两个 Microsoft List + Power Automate) | 记录每个账户状态;PM 整理 exception;Director 审批 exception |
| **Reporting** | Power BI(直接接 Tracker 的 Main Table) | GA / GTP 看板 + exception 明细与汇总 |

顶栏两个按钮:

- **Activity log** —— 右侧抽屉,按时间倒序记录每一步操作(= 写进 Tracker 的动作)。
- **Reset demo** —— 一键把所有数据恢复到初始状态。

每个 Tab 下面有一排**角色/视图切换**:
- LBP:`Sale` / `Pricer` / `Profit Mgr` / `Director / CFO` / `RPT records`
- Tracker:`Sale` / `Profit Mgr` / `Director / CFO`
- Reporting:`GA` / `GTP · by vertical`

切角色 = 切换"我现在是谁在用这个系统"。

---

## 2. 核心概念(先看这个)

| 概念 | 含义 |
|---|---|
| **Bid Type** | Sale 提单时选:`Repricing`(即 RPG,常规按指引调价)或 `Exception`(需要上报审批) |
| **Uplift** | Sale 和客户谈完后填:`Full Uplift` / `Partial Uplift` / `No Uplift` |
| **Partial Uplift → Partial Exception** | 客户只接受部分涨价 = 一种 exception |
| **No Uplift → Full Exception** | 客户完全不涨价 = 一种 exception |
| **RPT** | 嵌在 LBP 里的算价工具。只**算价 + 存记录**,**不做审批**。只有 Pricer / PM / Director 能打开,Sale 打不开(🔒) |
| **审批在哪做** | Repricing 由 **Pricer 在 LBP** 批;Exception 由 **Director 在 LBP 或 Tracker** 批。点 Approve/Reject 之后才弹出写 comment 的框,这段 comment Sale 看得到 |
| **Sync to Tracker** | LBP → Tracker 之间的一个轻量弹窗表单(Exception 提交时、Sale 确认 Repricing 结果时触发) |

---

## 3. LBP · RPT Tab

### 3.1 Sale —— 提单 + 确认

**提新单**(左侧 "New bid request"):

1. 选 `Sales Rep book` → `Account`(自动过滤到这个 manager 名下的账户)
2. `Bid Type`:`Repricing` 或 `Exception`
3. `Bid Request Type`(Renewal / New Business)、`Fulfillment Method`(Direct / Indirect)、`Description`
4. **Submit bid request**
   - **Repricing** → 单据进 Pricer 的 work list
   - **Exception** → 立刻弹 **Sync to Tracker** 窗口 → 填 `Active?` / `Uplift the customer will take`(Partial / No Uplift)/ `Valid date` / `LNL` / 原因 / requested vs guided → 提交后账户在 Tracker 变成 exception,PM 从 LBP 领单

**My Work List**(右侧):Pricer/Director 处理完、需要 Sale 关注的单。

- 点一行 → 进**单据详情页**
- 已批的 Repricing 单会显示 **Confirm & Sync to Tracker**:点开弹窗填
  - `Active?`
  - `Uplift agreed`:
    - **Full Uplift** → 普通 Repricing,结束
    - **Partial Uplift** → 变 **Partial Exception**,转 PM
    - **No Uplift** → 变 **Full Exception**,转 PM
  - `Q10 Valid date`、`LNL`
- 已出结果的单显示 **Open result**,点开能看到 Pricer / Director 留的 feedback comment

> Sale 在单据详情页看不到 RPT / LPS 按钮(🔒 提示)。

### 3.2 Pricer —— 算价 + 审批 Repricing

**My Work List**:上面是待处理队列(`with Pricer`),下面是所有 Repricing 单的总表。点任意一行进详情页。

**单据详情页**包含:

- Bid summary(ID / 账户 / geo / vertical / bid request type / fulfillment / 提单人 / description)
- **Bid lines** 表(PN / Memory·SSD / Prev / New / Margin)+ blended margin
- 工具行:**Open RPT** / **Open LPS**
- **Actions**:
  - **Approve** / **Reject** / **Reassign**
  - 点了之后**才**弹出写 comment 的界面(其他时候没有输入框)
  - comment 存在单据上,Sale 能看到
- Comment thread(历史)

**RPT**(从详情页 Open RPT 进,左上角 "← Back to bid" 返回):

1. 看到自动化流程 checklist → 点 **Run**
2. Run 完出**逐行结果表**:
   - 每行的 New price 都可以直接改,那一行的 margin 和底部 blended margin **实时重算**
   - RTX 等特殊 CPU 行标 `manual`,要手填价格才算 margin
3. 下面是**自动生成的 pricing note**(每行:`PN | prev price | memory | SSD (CPU) = new price` + blended margin)
   - **⧉ Copy pricing note** —— 一键复制到剪贴板
   - `Extra notes` 里补充的文字会拼进 note
4. **↔ Sync to LPS**(把算好的价推回 LPS,备注写"写入权限待和平台确认")
5. **Save record** —— 存成一条 RPT record(审计留痕)。RPT 到此为止,**不批单**

**LPS**(从详情页 Open LPS 进):只读 —— 每行的 LPS 合同价 vs RPT 新价 + 差额。

### 3.3 Profit Mgr —— 处理 Exception(LBP 这边)

**My Work List**:`with PM` 的 exception 单。点进详情页。

- **Open RPT**:PM 在这里重算内部价格(和 Pricer 用的是同一个 RPT)
- **Reassign to Director**:内部价改完,把单转给 Director

> Exception 的**案例内容**(past performance、材料、Save / Send)是在 **Tracker** 里填的,不在 LBP。

### 3.4 Director / CFO —— 审批 Exception(LBP 这边)

**My Work List**:`with Director` 的 exception 单。点进详情页。

- **Open RPT / LPS**:查看算价
- **Approve exception** / **Reject exception**:点了之后弹写 note 的界面,和 Tracker 上是同一个决定
- **Reassign to PM**:退回给 PM 继续处理

### 3.5 RPT records

所有 **Save record** 过的记录汇总表:`Record ID` / `Bid` / `Account` / `Saved by` / `Role` / `When`。

- 顶部搜索框可按 id / 账户 / bid / 角色过滤
- 点一行看完整 note,可复制

---

## 4. Tracker Tab

### 4.1 Sale —— 看板(只读自己填的)

- `Sale Manager` 选择器 + **+ Add new**
- 表格列:`Account` · `Active` · `Uplift` · `Valid date` · `LNL` · `Prev Sale` · `PM` · `Status`
- 点一行 → 详情:自己在 LBP 填的信息 + 上一个负责的 Sale / PM + 账户状态 + Pricer/Director 的 feedback comment + exception 结果(如已决定)
- Sale 这一页**不显示 CA No.、不显示财务数字**,也不能审批 —— 只是看

**+ Add new**(某些单没从 LBP 同步过来时手动补):
- `Account` 只能从 **Main Table 已有账户**里选,不能手打
- `Bid Type` / `Bid Request Type` / `Active` / `Uplift` / `Valid date` / `LNL` / `Description`

### 4.2 Profit Mgr —— 整理并上报 Exception

左侧**颜色看板**(黄色排最前):

| 颜色 | 含义 |
|---|---|
| 🟡 黄 | exception 等 PM 处理 |
| 🔵 蓝 | Repricing 账户 |
| 🟢 绿 | exception 已批 |
| 🔴 红 | exception 被拒 |
| ⚪ 灰 | 在别处流转中 |

**+ Add new**(PM 版比 Sale 版多):`CA No.` + `Exc REV $(M)` / `GP $(M)` / `GP Impact $(M)`(留空则自动估算)。

点账户进详情:

- **Account & exception info** 块 —— **可编辑**:`Active` / `Uplift` / `Exception class`(自动推导)/ `Valid date` / `LNL` / `CA No.` / `Exc REV / GP / GP Impact`。PM / Director 可以改 Sale 填的内容;REV/GP/GP Impact 默认按 bid line + uplift gap 估算,可覆盖成真实数字
- `Stage` / `Requested` / `Draft` 状态
- `Sale's reason`
- **Account past performance**(给 CFO 看的理由)—— 文本框
- **Materials** 列表 + `+ Attach TMCGP bridge`
- 按钮(在 `PM-intake` 阶段):
  - **Save draft** —— 存草稿
  - **Send to Director** —— 送去 Director 的 Tracker(送之前必须填了 past performance)
  - **Reject to Sale** —— 退回给 Sale
- 送出后显示"awaiting decision";Director 决定后这里会显示结果 + **Mark read**

如果点到的是 Repricing 账户:有一个 **Flag as Exception** 按钮可手动标记为 exception。

### 4.3 Director / CFO —— 审批 Exception + KPI 计算器

左侧颜色看板同上(黄色 = `Director-review` 待审)。

点账户进详情:

- **Account & exception info** 块(同 PM,可编辑)
- `Sale's reason` + PM 的 `past performance` + `Materials`
- **KPI impact calculator**:
  - 上方显示**baseline 数据源**(按 Geo × vertical 的 REV / TMCGP / TMCGP%)
  - 中间可加行,每行填 `Units` / `REV (USD)` / `TMCGP%`
  - **+ Row** / **Calculate** → 出影响表:`Base` / `New deal` / `Total` / `Δ` / `B(W) vs MT`
- **Approve exception** / **Reject exception**:点了之后弹写 note 的界面 → 决定同步回 Tracker,PM + Sale 收到未读通知

---

## 5. Reporting Tab

- 右上角一个大号 **Report date** 卡片 —— 打开网页当天的实际日期
- 顶部 stat 卡:`GA accounts` / `Active` / `Not active` / `Missing input` + **Partial Exception** / **Full Exception** 两张 sizing 卡(账户数 · Rev · GP · GP Impact)

**GA 页**:GA account 分解树(总量 → Active / Not active → Full / Partial / No uplift / Missing input)+ 右侧 uplift 桶表。

**GTP 页**:同样的账户按 vertical 拆分(堆叠条)+ uplift 桶表。

**Exception & missing-input 明细**(两个页都有):

- 筛选:`Geo` / `Vertical` / `Exception class`(All / Partial Exception / Full Exception)+ `Clear`
- **Exception 表**:`HQ Geo` · `Account` · `ISR` · `GAM` · `Class` · `Q10 Valid Date` · `CA No.` · `Exc REV $(M)` · `GP $(M)` · `GP Impact $(M)` · `Stage / Decision`
  - 底部 **Total 行**:按当前筛选汇总 REV / GP / GP Impact,并注明口径
- **Missing input 表**:`Account` · `Geo/Vert` · `ISR` · `Sale Mgr` · `Last updated`

> PM / Director 在 Tracker 里改的 CA No. 和 REV/GP/GP Impact 会实时反映到这张表和 Total。

---

## 6. 建议的三条演示路线

打开网页后,建议照这三条各走一遍,能覆盖所有分支:

### 路线 A —— 常规 Repricing(Full Uplift)

1. **LBP · Sale**:选账户 → Bid Type `Repricing` → Submit
2. **LBP · Pricer**:My Work List 点开这条单 → **Open RPT** → **Run** → (可改价看 margin)→ **Save record** → **← Back to bid**
3. 详情页点 **Approve** → 写一句 comment → Confirm
4. **LBP · Sale**:My Work List 点开 → **Confirm & Sync to Tracker** → `Uplift agreed` 选 **Full Uplift** → 提交
5. **Tracker · Sale**:账户状态变 "Repricing — done"
6. **Reporting**:该账户进 "Full uplift" 桶

### 路线 B —— 通过 uplift 变成 Exception

1~3 步同上(Pricer 批掉 Repricing 单)
4. **LBP · Sale**:Confirm 时 `Uplift agreed` 选 **Partial Uplift**(或 No Uplift)
5. 账户自动变 **Partial Exception**(或 Full Exception),进 PM 看板(黄色)
6. **Tracker · Profit Mgr**:点该账户 → 填 **past performance** → (可改 REV/GP/GP Impact、CA No.)→ **Send to Director**
7. **Tracker · Director / CFO**:点该账户 → 填 KPI 计算器 → **Calculate** → **Approve exception** → 写 note
8. **Tracker · Sale** / **Profit Mgr**:该账户出现未读通知,点开看结果
9. **Reporting**:exception 表里能看到它,用 `Exception class` 筛选 + 看 Total

### 路线 C —— 一开始就走 Exception

1. **LBP · Sale**:Bid Type 直接选 **Exception** → Submit
2. 立刻弹 **Sync to Tracker** → 填 Active / Uplift / Valid date / LNL / 原因 → 提交
3. **LBP · Profit Mgr**:My Work List 领这条单 → **Open RPT** 重算内部价 → **Reassign to Director**
4. **Tracker · Profit Mgr**:补 past performance / 材料 → **Send to Director**
5. **Tracker · Director / CFO**:KPI 计算器 → **Approve / Reject** → 同步回 Sale + PM

---

## 7. 其他说明

- **改数据不会存**:刷新页面或点 Reset demo 就回到初始状态。
- **想改文案 / 账户 / 颜色**:直接编辑 `index.html`(HTML + CSS + JS 全在一个文件里),`git push` 后 GitHub Pages 自动重新部署。
- **深色模式**:跟随系统 / 浏览器主题。
- 同一个原型也发布在 Claude Artifact(需登录 claude.ai 才能看):<https://claude.ai/code/artifact/63a32f45-6dcc-40bd-badb-f4bc5b5d12c2>
